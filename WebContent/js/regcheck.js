/**
 * 
 */

idcheck = function(){
   // 1.공백
   
   // 2.길이
   if(길이 < 4 || 길이 > 12){
      alert("id는 4~12사이입니다");
      return false;
   }
   3.
   입력값변수 ;
   정규식패턴 ;
   // 입력값.match(정규식) - 일치하면 일치되는 값을 리턴 없으면 null을 return
   // 정규식.test(입력값) - 일치하면 true를 리턴 않으면 false를 리턴
   if(!(수식)){
      alert('오류메시지')
      return false;
   }
   
   return true;
}

datacheck = function(){
   // pass, name, hp, mail - 공백 검증과 길이검증, 정규식
   // 이름에 대하여
   // 공백체크
   if("이름공백?"){
      alert("name 입력");
      return false;
   }
   
   if(!(name수식)){
      if("한글?"){
// 길이체크 - return false
         // 정규식체크 - return false
         
      }else if("영문"){
// 길이체크 - return false
         // 정규식체크 - return false
      }
      alert("name 입력");
      return false;
   }
   // 비밀번호에 대하여
   // 공백체크
   // ..길이체크
   if(!(pass정규식)){
      alert('오류메세지')
      return false;
   }
   
   // hp번호에 대하여
   // 1.공백체크
   // 2.정규식체크↓
   if(!(hp수식)){
      alert('오류메세지')
      return false;
   }
   
   // mail에 대하여
   if(!(mail수식)){
      alert('오류메세지')
      return false;
   }
   return true;
   // pass, name, hp, mail - 공백검증과 길이검증, 정규식 체크
   
}